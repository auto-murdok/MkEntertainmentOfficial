using UnityEngine;

/// <summary>Legacy alias — kept for asset compatibility. New code can use <see cref="EventChannel"/> directly.</summary>
[CreateAssetMenu(fileName = "VoidEventChannel", menuName = "Game/Events/Void Event Channel")]
public class VoidEventChannel : EventChannel { }
